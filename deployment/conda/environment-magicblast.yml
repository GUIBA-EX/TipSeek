name: tipseek-magicblast
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  # Magic-BLAST 1.7.0 requires zlib 1.2.x, whereas current minimap2 uses
  # zlib 1.3.x. Isolate it so both supported TipSeek routes remain usable.
  - magicblast=1.7.0
